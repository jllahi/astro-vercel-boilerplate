---
interface Props {
  header?: string
  // body: string;
  footer?: string
  href?: string
}

const { header, footer, href } = Astro.props

// <li class="link-card">
// 	<a href={href}>
// 		<h2>
// 			{title}
// 			<span>&rarr;</span>
// 		</h2>
// 		<p>
// 			{body}
// 		</p>
// 	</a>
// </li>
---

<article>
  {
    header && (
      <header class="text-xl font-black">
        <h3>{href ? <a href={href}>{header}</a> : { header }}</h3>
      </header>
    )
  }
  <slot />
  {footer && <footer>{footer}</footer>}
</article>
